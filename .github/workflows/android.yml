name: Build Android APK

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: set up JDK 17
      uses: actions/setup-java@v3
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Setup Android SDK
      uses: android-actions/setup-android@v3

    - name: Setup Gradle
      uses: gradle/actions/setup-gradle@v3
      with:
        gradle-version: '8.5' # Explicitly use a specific Gradle version since we don't have a wrapper

    - name: Build Debug APK
      run: gradle assembleDebug --stacktrace

    - name: Upload Debug APK
      uses: actions/upload-artifact@v3
      with:
        name: OmniAPK-debug
        path: app/build/outputs/apk/debug/app-debug.apk

    # Optional: Release build if secrets are configured
    # - name: Build Release APK
    #   if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
    #   run: |
    #     echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 --decode > app/keystore.jks
    #     gradle assembleRelease \
    #       -Pandroid.injected.signing.store.file=$(pwd)/app/keystore.jks \
    #       -Pandroid.injected.signing.store.password=${{ secrets.KEYSTORE_PASSWORD }} \
    #       -Pandroid.injected.signing.key.alias=${{ secrets.KEY_ALIAS }} \
    #       -Pandroid.injected.signing.key.password=${{ secrets.KEY_PASSWORD }}
    #
    # - name: Upload Release APK
    #   if: ${{ secrets.ANDROID_KEYSTORE_BASE64 != '' }}
    #   uses: actions/upload-artifact@v3
    #   with:
    #     name: OmniAPK-release
    #     path: app/build/outputs/apk/release/app-release.apk
